We're using the [http://git.or.cz/ git] version control system to manage the source code for this project.  To obtain a copy of the source code, install the latest version of git and then issue the following command:

`git clone git://72.51.43.91/pub/repos/hypertable.git`

This git repository represents the most up-to-date version of the source code.  We push changes into this repository only if all of the regression tests pass.  To update your local git repository (created with the above command) with the latest changes, issue the following command:

`git pull ssh://72.51.43.91/pub/repos/hypertable.git`